import React, { createContext } from 'react';

import { RecoilRoot } from 'recoil';

const App: React.FC = () => (
  <RecoilRoot>
    <h1>Template Vite React TS (Recoil state managed) </h1>
  </RecoilRoot>
);

export default App;
